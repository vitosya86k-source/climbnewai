# СРОЧНОЕ ИСПРАВЛЕНИЕ — Визуализация на видео и дашборд

**Проблема:** Текущая реализация не соответствует спецификации. Паутинка нечитаемая, дашборд не отправляется.

---

## 🔴 ПРОБЛЕМА 1: Паутинка на видео слишком мелкая и без подписей

### Что сейчас:
- Паутинка ~50px, еле видно
- Нет подписей осей (QF, HP, ПВ, СЧ, РТ, ДН, GR)
- Нет значений метрик
- Нет шкалы (0, 25, 50, 75, 100)

### Что нужно:

**Размер и позиция:**
```python
SPIDER_CONFIG = {
    "position": (20, 20),      # отступ от верхнего левого угла
    "radius": 100,             # радиус паутинки в пикселях (было ~30, нужно 100)
    "center_x": 120,           # центр = position + radius
    "center_y": 120,
}
```

**Обязательные элементы:**
1. Фоновые круги шкалы (25%, 50%, 75%, 100%)
2. 7 осей с линиями от центра
3. Подписи осей СНАРУЖИ паутинки: QF, HP, ПВ, СЧ, РТ, ДН, GR
4. Заполненный полигон с данными
5. Точки на вершинах полигона
6. Общий балл под паутинкой: "Score: 66"

**Код отрисовки (OpenCV):**
```python
def draw_spider_chart(frame, metrics: dict):
    """
    Отрисовка паутинки на кадре видео.
    
    metrics: {"quiet_feet": 78, "hip_position": 52, "diagonal": 94, ...}
    """
    import math
    import numpy as np
    
    # Конфигурация
    center_x, center_y = 120, 120
    radius = 100
    num_axes = 7
    angle_step = 2 * math.pi / num_axes
    
    # Названия осей
    axes = ["QF", "HP", "ПВ", "СЧ", "РТ", "ДН", "GR"]
    metric_keys = ["quiet_feet", "hip_position", "diagonal", "route_reading", 
                   "rhythm", "dynamic_control", "grip_release"]
    
    # Создаём overlay для прозрачности
    overlay = frame.copy()
    
    # 1. Фоновый круг (тёмный)
    cv2.circle(overlay, (center_x, center_y), radius + 10, (30, 30, 30), -1)
    
    # 2. Круги шкалы (25%, 50%, 75%, 100%)
    for pct in [25, 50, 75, 100]:
        r = int(radius * pct / 100)
        cv2.circle(overlay, (center_x, center_y), r, (60, 60, 60), 1)
    
    # 3. Оси и подписи
    points = []
    for i, (axis_name, metric_key) in enumerate(zip(axes, metric_keys)):
        angle = -math.pi/2 + i * angle_step  # начинаем сверху
        
        # Конец оси
        end_x = int(center_x + radius * math.cos(angle))
        end_y = int(center_y + radius * math.sin(angle))
        
        # Рисуем ось
        cv2.line(overlay, (center_x, center_y), (end_x, end_y), (80, 80, 80), 1)
        
        # Подпись оси (снаружи)
        label_x = int(center_x + (radius + 25) * math.cos(angle))
        label_y = int(center_y + (radius + 25) * math.sin(angle))
        cv2.putText(overlay, axis_name, (label_x - 10, label_y + 5),
                    cv2.FONT_HERSHEY_SIMPLEX, 0.5, (200, 200, 200), 1)
        
        # Точка значения
        value = metrics.get(metric_key, 50)
        if value is None:
            value = 50
        value = max(0, min(100, value))  # clamp 0-100
        
        point_r = radius * value / 100
        point_x = int(center_x + point_r * math.cos(angle))
        point_y = int(center_y + point_r * math.sin(angle))
        points.append((point_x, point_y))
    
    # 4. Заполненный полигон
    if len(points) >= 3:
        pts = np.array(points, np.int32)
        cv2.fillPoly(overlay, [pts], (255, 200, 0, 100))  # жёлто-оранжевый
        cv2.polylines(overlay, [pts], True, (255, 255, 255), 2)
    
    # 5. Точки на вершинах
    for point in points:
        cv2.circle(overlay, point, 5, (255, 255, 255), -1)
    
    # 6. Общий балл
    avg_score = sum(metrics.get(k, 50) or 50 for k in metric_keys) / len(metric_keys)
    cv2.putText(overlay, f"Score: {int(avg_score)}", 
                (center_x - 30, center_y + radius + 45),
                cv2.FONT_HERSHEY_SIMPLEX, 0.6, (255, 255, 255), 2)
    
    # Применяем прозрачность
    alpha = 0.85
    cv2.addWeighted(overlay, alpha, frame, 1 - alpha, 0, frame)
    
    return frame
```

---

## 🔴 ПРОБЛЕМА 2: Дашборд не отправляется вместе с видео

### Что должно происходить:

После обработки видео бот должен отправить:
1. **Обработанное видео** (со скелетом и паутинкой)
2. **Дашборд PNG** (SWOT-анализ, метрики, паутинка)

### Где искать проблему:

**В `handlers.py`** — после вызова `processor.process_video()`:

```python
async def send_results(update: Update, context: ContextTypes.DEFAULT_TYPE, result: dict):
    """Отправка результатов пользователю"""
    chat_id = update.effective_chat.id
    
    # 1. Отправляем видео
    video_path = result.get('processed_video_path')
    if video_path and os.path.exists(video_path):
        await context.bot.send_video(
            chat_id=chat_id,
            video=open(video_path, 'rb'),
            caption="🎬 Обработанное видео с анализом техники"
        )
    
    # 2. ОБЯЗАТЕЛЬНО отправляем дашборд
    dashboard_path = result.get('dashboard_path')
    if dashboard_path and os.path.exists(dashboard_path):
        await context.bot.send_photo(
            chat_id=chat_id,
            photo=open(dashboard_path, 'rb'),
            caption="📊 SWOT-анализ техники лазания"
        )
    else:
        # Если дашборд не сгенерировался — логируем ошибку!
        logger.error(f"Dashboard not found: {dashboard_path}")
        await context.bot.send_message(
            chat_id=chat_id,
            text="⚠️ Дашборд не сгенерирован. Проверьте логи."
        )
```

**В `processor.py`** — проверить что дашборд генерируется:

```python
def process_video(self, video_path: str, ...) -> dict:
    # ... обработка кадров ...
    
    # После обработки всех кадров — ГЕНЕРИРУЕМ ДАШБОРД
    try:
        dashboard_path = self.dashboard_generator.generate_dashboard(
            technique_metrics=technique_metrics,
            additional_metrics=additional_metrics,
            swot=swot_data,
            estimated_grade=grade,
            video_duration=duration,
            total_frames=total_frames
        )
        logger.info(f"✅ Dashboard generated: {dashboard_path}")
    except Exception as e:
        logger.error(f"❌ Dashboard generation failed: {e}")
        dashboard_path = None
    
    return {
        'processed_video_path': output_video_path,
        'dashboard_path': dashboard_path,  # ОБЯЗАТЕЛЬНО ВЕРНУТЬ!
        # ... остальные данные ...
    }
```

---

## 🔴 ПРОБЛЕМА 3: Цветной скелет — правая рука оранжевая, но логика неясна

### Что вижу на скриншоте:
- Большая часть скелета зелёная
- Правое плечо и предплечье оранжевые (напряжение)

### Что нужно проверить:

```python
def get_skeleton_color(tension_value: float) -> tuple:
    """
    tension_value: 0-100
    
    Returns: BGR color tuple
    """
    if tension_value < 30:
        return (0, 255, 0)    # Зелёный — норма
    elif tension_value < 60:
        return (0, 165, 255)  # Оранжевый — внимание
    else:
        return (0, 0, 255)    # Красный — зажим
```

Убедиться что:
1. `tension_analyzer.py` возвращает значения 0-100 для каждой зоны
2. Цвета применяются к правильным сегментам скелета
3. Нет инверсии цветов (BGR vs RGB)

---

## ✅ ЧЕКЛИСТ ДЛЯ CURSOR

### Паутинка на видео:
- [ ] Увеличить радиус до 100px
- [ ] Добавить подписи осей (QF, HP, ПВ, СЧ, РТ, ДН, GR)
- [ ] Добавить круги шкалы (25, 50, 75, 100%)
- [ ] Добавить "Score: XX" под паутинкой
- [ ] Проверить что значения не None (заменять на 50)

### Дашборд:
- [ ] Проверить что `generate_dashboard()` вызывается
- [ ] Проверить что путь к дашборду возвращается в result
- [ ] Проверить что файл существует перед отправкой
- [ ] Добавить `send_photo()` для дашборда в handlers.py
- [ ] Добавить логирование ошибок

### Тестирование:
- [ ] Загрузить тестовое видео
- [ ] Проверить что паутинка читаемая (подписи видны)
- [ ] Проверить что приходит И видео И дашборд
- [ ] Проверить логи на ошибки

---

## 📁 ФАЙЛЫ ДЛЯ РЕДАКТИРОВАНИЯ

1. `app/video/overlays.py` — метод отрисовки паутинки
2. `app/video/processor.py` — генерация дашборда и возврат пути
3. `app/bot/handlers.py` — отправка дашборда пользователю
4. `app/reports/dashboard.py` — проверить что генерация работает

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После исправлений пользователь получает:

1. **Видео** с:
   - Цветным скелетом (зелёный/жёлтый/красный)
   - Большой читаемой паутинкой в углу (7 осей с подписями)
   - Общим баллом под паутинкой

2. **Дашборд PNG** с:
   - Заголовком ClimbAI
   - Списком 7 метрик с прогресс-барами
   - Паутинкой и общим баллом
   - SWOT-сеткой (4 блока)
   - Дополнительными метриками
   - Footer с методологией
