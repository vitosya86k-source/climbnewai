# ClimbAI BoulderVision - Полное описание системы

**Дата создания:** 21 января 2026  
**Версия:** MVP 1.0 (новая концепция)  
**Статус:** В разработке, новая архитектура на основе ALGORITHM_SPEC.md

> ⚠️ **ВАЖНО:** Система переходит на новую концепцию с 7 базовыми метриками техники и алгоритмическим анализом без ИИ. Текущая реализация частично работает, но требует переработки в соответствии с новой спецификацией.

## 🔄 ПЕРЕХОД НА НОВУЮ КОНЦЕПЦИЮ

### Текущее состояние vs Новая концепция

| Аспект | Текущая реализация | Новая концепция (MVP 1.0) |
|--------|-------------------|---------------------------|
| **Метрики паутинки** | 4 метрики: Сила, Баланс, Координация, Техника | 7 метрик: Quiet Feet, Hip Position, Противовес, Считывание, Ритм, Динамика, Grip Release |
| **Источник метрик** | Эмпирические формулы | Методология Eric J. Hörst «Training for Climbing» |
| **Текстовые отчёты** | AI генерация (Claude) | Алгоритмические шаблоны из `TEXT_TEMPLATES.md` |
| **Дашборд** | Matplotlib с базовыми графиками | Структурированный дашборд с SWOT-анализом |
| **Визуализация** | Базовый скелет, паутинка с 4 осями | Цветной скелет по зонам напряжения, паутинка с 7 осями |
| **SWOT-анализ** | Не реализован | Алгоритмический SWOT на основе метрик и шаблонов |

### Файлы новой концепции

В папке `files/` находятся спецификации новой концепции:

- **`ALGORITHM_SPEC.md`** (1220 строк) — Полная спецификация:
  - 7 базовых метрик техники с алгоритмами расчёта
  - Дополнительные метрики (стабильность, истощение, эффективность)
  - Визуализация: цветной скелет и паутинка
  - SWOT-анализ: логика формирования
  - Дашборд: структура и компоненты
  - Подключение ИИ: когда и как использовать

- **`TEXT_TEMPLATES.md`** (700 строк) — Текстовые шаблоны:
  - Шаблоны для всех 7 базовых метрик (4 уровня каждый)
  - Шаблоны для дополнительных метрик
  - Шаблоны для SWOT (Strengths, Weaknesses, Opportunities, Threats)
  - Шаблоны для угроз (риски травм)
  - Алгоритм выбора текста на основе балла

- **`dashboard_prototype.html`** (682 строки) — Прототип дашборда:
  - Визуальный макет с CSS
  - Структура: Header, Техника, SWOT, Дополнительные метрики, Footer
  - Примеры данных для демонстрации
  - Адаптивный дизайн

### Что нужно сделать

1. ✅ **Спецификация готова** — `files/ALGORITHM_SPEC.md`
2. ✅ **Шаблоны готовы** — `files/TEXT_TEMPLATES.md`
3. ✅ **Прототип готов** — `files/dashboard_prototype.html`
4. ⚠️ **Требуется реализация:**
   - Новые модули для 7 метрик (`app/analysis/technique_metrics.py`)
   - Обновление визуализации (`app/video/overlays.py`)
   - Обновление дашборда (`app/reports/dashboard.py`)
   - Интеграция шаблонов (`app/reports/text_templates.py`)

---

## 📋 СОДЕРЖАНИЕ

1. [Общая архитектура](#общая-архитектура)
2. [Компоненты системы](#компоненты-системы)
3. [Типы визуализации](#типы-визуализации)
4. [Поток обработки видео](#поток-обработки-видео)
5. [Метрики и расчеты](#метрики-и-расчеты)
6. [Известные проблемы](#известные-проблемы)
7. [Взаимодействие компонентов](#взаимодействие-компонентов)

---

## 🏗️ ОБЩАЯ АРХИТЕКТУРА

### Структура проекта

```
telegram_bot_bouldervision/
├── app/
│   ├── main.py                    # Точка входа бота
│   ├── config.py                  # Конфигурация (API ключи, настройки)
│   ├── bot/                       # Telegram бот
│   │   ├── handlers.py           # Обработчики команд и callback'ов
│   │   ├── keyboards.py          # Клавиатуры для бота
│   │   └── messages.py           # Шаблоны сообщений
│   ├── video/                     # Обработка видео
│   │   ├── processor.py          # Главный процессор видео
│   │   ├── overlays.py           # Система визуализации (1334 строки)
│   │   └── ghost_manager.py      # Управление эталонными видео
│   ├── analysis/                  # Алгоритмический анализ
│   │   ├── frame_analyzer.py     # Анализ каждого кадра
│   │   ├── tension_analyzer.py   # Анализ напряжения/зажимов
│   │   ├── fall_detector.py      # Детекция падений
│   │   ├── injury_predictor.py   # Прогноз травм
│   │   ├── nine_box_model.py     # HR модель (навыки/потенциал)
│   │   ├── route_assessor.py     # Оценка трассы
│   │   └── algorithmic.py       # Алгоритмический отчет без AI
│   ├── bouldervision/            # BoulderVision метрики
│   │   ├── metrics.py            # Velocity Ratio, Trajectory Similarity
│   │   └── holds_detector.py    # Детекция зацепов (Roboflow)
│   ├── reports/                  # Генерация отчетов
│   │   ├── generator.py         # AI отчеты (Claude)
│   │   ├── dashboard.py         # Дашборд с метриками (matplotlib)
│   │   └── prompts.py          # Промпты для AI
│   ├── database/                 # База данных
│   │   ├── models.py            # SQLAlchemy модели
│   │   ├── crud.py              # CRUD операции
│   │   └── session.py           # Сессии БД
│   └── utils/                    # Утилиты
│       └── logger.py            # Настройка логирования
├── data/
│   ├── ghosts/                   # Эталонные видео (JSON)
│   ├── temp/                     # Временные файлы
│   └── exports/                  # Экспорты
└── run_bot.py                    # Скрипт запуска бота
```

### Технологический стек

- **Python 3.12**
- **MediaPipe** - детекция позы (33 ключевые точки)
- **OpenCV** - обработка видео и визуализация
- **Telegram Bot API** - интерфейс бота
- **Anthropic Claude** - AI анализ и рекомендации
- **Matplotlib** - генерация дашбордов
- **SQLAlchemy** - база данных
- **Roboflow** (опционально) - детекция зацепов

---

## 🧩 КОМПОНЕНТЫ СИСТЕМЫ

### 1. Telegram Bot (`app/bot/`)

#### handlers.py
**Назначение:** Обработка всех команд и callback'ов от пользователя

**Основные функции:**
- `start_command()` - команда /start
- `handle_video()` - обработка загруженного видео
- `handle_overlay_selection()` - выбор типа визуализации
- `handle_report_selection()` - выбор формата отчета
- `handle_next_actions()` - действия после получения результата

**Поток работы:**
1. Пользователь отправляет видео → сохраняется в `context.user_data['video_path']`
2. Пользователь выбирает визуализацию → сохраняется в `context.user_data['selected_overlay']`
3. Нажимает "✅ Готово" → вызывается `VideoProcessor.process_video()`
4. Результат отправляется пользователю

#### keyboards.py
**Назначение:** Определение клавиатур для бота

**Клавиатуры:**
- `get_overlay_selection_keyboard()` - выбор визуализации (5 типов + полный анализ)
- `get_report_format_keyboard()` - выбор формата отчета (8 стилей)
- `get_next_actions_keyboard()` - действия после обработки

**Типы визуализации в клавиатуре:**
- `overlay_full` - Полный анализ
- `overlay_spider_metrics` - Метрики (паутинка)
- `overlay_weight_load` - Нагрузка
- `overlay_tension_zones` - Зажимы
- `overlay_speed_map` - Скорость
- `overlay_ideal_ghost` - Призрак-эталон

---

### 2. Video Processor (`app/video/processor.py`)

**Класс:** `VideoProcessor`

**Назначение:** Главный процессор видео, координирует все анализаторы

**Инициализация:**
```python
- MediaPipe Pose (детекция скелета)
- FrameAnalyzer (анализ кадров)
- FallDetector (детекция падений)
- VideoOverlays (визуализация)
- BoulderVisionMetrics (метрики движения)
- BodyTensionAnalyzer (анализ напряжения)
- InjuryPredictor (прогноз травм)
- ClimberNineBoxModel (HR модель)
- RouteAssessor (оценка трассы)
- HoldsDetector (детекция зацепов, опционально)
```

**Метод `process_video()`:**
1. Открывает видео файл
2. Для каждого кадра:
   - Детектирует позу через MediaPipe
   - Анализирует кадр через `FrameAnalyzer`
   - Вычисляет BoulderVision метрики
   - Обновляет историю в `VideoOverlays`
   - Применяет выбранную визуализацию
   - Записывает кадр в выходное видео
3. После обработки всех кадров:
   - Генерирует статистику
   - Выполняет алгоритмические анализы
   - Генерирует дашборд (если возможно)
   - Возвращает результат

**Возвращаемые данные:**
```python
{
    'processed_video_path': str,      # Путь к обработанному видео
    'csv_path': str,                 # Путь к CSV с данными
    'dashboard_path': str,           # Путь к дашборду (или None)
    'duration': float,               # Длительность видео
    'total_frames': int,              # Всего кадров
    'avg_pose_quality': float,       # Среднее качество позы
    'avg_motion_intensity': float,   # Средняя интенсивность движения
    'fall_detected': bool,           # Обнаружено ли падение
    'bouldervision': {...},          # BoulderVision метрики
    'tension_analysis': {...},       # Анализ напряжения
    'injury_prediction': {...},      # Прогноз травм
    'nine_box': {...},               # HR модель
    'holds_analysis': {...}          # Анализ зацепов
}
```

---

### 3. Video Overlays (`app/video/overlays.py`)

**Класс:** `VideoOverlays`

**Назначение:** Система визуализации, наложение графики на видео

**Состояние (история):**
- `metrics_history` - история метрик (последние 90 кадров)
- `position_history` - история позиций ключевых точек
- `tension_history` - история напряжения по зонам
- `velocity_history` - история скорости движения
- `ideal_landmarks_sequence` - эталонная последовательность (призрак)
- `current_frame_idx` - текущий индекс кадра

**Метод `apply_overlay()`:**
Принимает кадр, landmarks, тип визуализации и применяет соответствующую функцию отрисовки.

**Типы визуализации:**

#### 3.1. `spider_metrics` - Паутинка метрик
**Функция:** `draw_spider_metrics()`

**Что показывает:**
- Радарная диаграмма с 4 осями: Сила, Баланс, Координация, Техника
- Значения от 0 до 100%
- Общий балл внизу

**Как вычисляется:**
- **Сила:** на основе углов локтей (оптимальный угол 90-120°)
- **Баланс:** отклонение центра бёдер от центра ног
- **Координация:** симметрия движений левой и правой стороны
- **Техника:** комбинация качества позы и интенсивности движения

**Проблемы:**
- ❌ Метрики могут быть None/NaN → показываются вопросительные знаки
- ❌ Значения не всегда вычисляются корректно
- ❌ Паутинка может быть пустой или с некорректными данными

**Код:** строки 446-579

---

#### 3.2. `weight_load` - Нагрузка на конечности
**Функция:** `draw_weight_load()`

**Что показывает:**
- Распределение нагрузки на 4 конечности: левая рука, правая рука, левая нога, правая нога
- Цветовая индикация: зелёный (<20%), жёлтый (20-35%), оранжевый (35-50%), красный (>50%)
- Размер круга пропорционален нагрузке

**Как вычисляется:**
- Функция `_calculate_weight_distribution()` (строки 677-730)
- На основе высоты конечностей относительно центра масс (бёдер)
- Нормализуется до 100%

**Что исправлено:**
- ✅ Теперь показывает ТОЛЬКО проценты (без кг)
- ✅ Заголовок изменён на "НАГРУЗКА (%)"
- ✅ Легенда показывает диапазоны в процентах

**Проблемы:**
- ❌ Внизу легенда может показывать вопросы (если значения некорректны)
- ❌ Распределение может быть неточным

**Код:** строки 581-675

---

#### 3.3. `tension_zones` - Зажимы и риски
**Функция:** `draw_tension_zones()`

**Что показывает:**
- Зоны напряжения: левое/правое плечо, левый/правый локоть, поясница, левое/правое колено
- Цветовая индикация по уровню напряжения
- Предупреждения о зажимах

**Как вычисляется:**
- Функция `_calculate_tension()` (строки 301-352)
- На основе углов суставов
- Оптимальные диапазоны:
  - Плечи: 60-120°
  - Локти: 80-150°
  - Поясница: 160-180°
  - Колени: 90-170°

**Проблемы:**
- ❌ Не всегда отображается корректно
- ❌ Значения могут быть None/NaN

**Код:** строки 760-865

---

#### 3.4. `speed_map` - Скорость и карта решений
**Функция:** `draw_speed_map()`

**Что показывает:**
- Спидометр со стрелкой (скорость движения)
- Точки раздумий (где человек останавливался)
- Размер точки = длительность остановки

**Как вычисляется:**
- Скорость: функция `_calculate_velocity()` (строки 368-386)
- На основе смещения ключевых точек между кадрами
- Точки раздумий: когда скорость < 0.005

**Проблемы:**
- ❌ Стрелка может быть некорректной
- ❌ Скорость может быть 0 или некорректной

**Код:** строки 867-1003

---

#### 3.5. `ideal_ghost` - Призрак-эталон
**Функция:** `draw_ideal_ghost()`

**Что показывает:**
- Текущий скалолаз (зелёный скелет)
- Эталонный вариант (синий полупрозрачный скелет, опережает на 10 кадров)

**Как работает:**
- Загрузка через `load_ghost_from_file()` (строки 78-98)
- Использует `GhostManager` для загрузки из JSON файлов
- Эталон должен быть загружен заранее

**Проблемы:**
- ❌ Не работает - призрак не загружается или не отображается
- ❌ Нет интерфейса для загрузки эталона через бота
- ❌ Формат данных может быть некорректным

**Код:** строки 732-740

---

#### 3.6. `full` - Полный анализ
**Функция:** `draw_full_analysis()`

**Что должно показывать:**
- Скелет с цветовой кодировкой напряжения
- Мини-паутинка метрик (левый верхний угол)
- Нагрузка в процентах (правый верхний угол)
- Скорость (верхний правый угол) - **ДОБАВЛЕНО**
- Зажимы (нижний левый угол) - **ДОБАВЛЕНО**
- Общая оценка (внизу по центру)

**Проблемы:**
- ❌ Скорость и зажимы добавлены в код, но могут не отображаться
- ❌ Паутинка может быть пустой
- ❌ Нагрузка может показывать вопросы

**Код:** строки 1005-1135

---

### 4. Frame Analyzer (`app/analysis/frame_analyzer.py`)

**Класс:** `FrameAnalyzer`

**Назначение:** Анализ каждого кадра видео

**Что вычисляет:**
- `pose_quality` - качество позы (0-100) на основе visibility ключевых точек
- `angles` - углы суставов (локти, плечи, бёдра, колени)
- `center_of_mass` - центр масс (x, y)
- `balance_score` - оценка баланса (0-100)
- `motion_intensity` - интенсивность движения между кадрами

**Метод `analyze_frame()`:**
Принимает номер кадра, landmarks, timestamp и возвращает словарь с метриками.

**Сохраняет:** Все данные в `self.frame_data` для последующего анализа.

---

### 5. BoulderVision Metrics (`app/bouldervision/metrics.py`)

**Класс:** `BoulderVisionMetrics`

**Назначение:** Вычисление метрик движения по методологии BoulderVision

**Метрики:**
- `velocity_ratio` - отношение скорости к оптимальной
- `cumulative_distance` - накопленное расстояние движения
- `trajectory_similarity` - схожесть траектории с предыдущими кадрами
- `trajectory_efficiency` - эффективность траектории (Stanford)
- `straight_arms_efficiency` - эффективность прямых рук

**Метод `process_frame()`:**
Обрабатывает каждый кадр и возвращает метрики движения.

**Метод `get_summary()`:**
Возвращает сводную статистику по всему видео.

---

### 6. Tension Analyzer (`app/analysis/tension_analyzer.py`)

**Класс:** `BodyTensionAnalyzer`

**Назначение:** Анализ мышечного напряжения и зажимов

**Что анализирует:**
- Напряжение в 7 зонах: левое/правое плечо, левый/правый локоть, поясница, левое/правое колено
- Уровень риска травм
- Моменты высокого напряжения

**Метод `analyze_frame()`:**
Анализирует каждый кадр и сохраняет данные о напряжении.

**Метод `get_summary()`:**
Возвращает сводку по напряжению, зоны риска, рекомендации.

---

### 7. Ghost Manager (`app/video/ghost_manager.py`)

**Класс:** `GhostManager`

**Назначение:** Управление эталонными видео (призраками)

**Функции:**
- `save_ghost()` - сохранение эталона в JSON
- `load_ghost()` - загрузка эталона из JSON
- `create_ghost_from_video()` - создание эталона из видео
- `calculate_similarity()` - расчёт схожести с эталоном

**Формат данных:**
JSON файл с последовательностью landmarks для каждого кадра.

**Проблемы:**
- ❌ Нет интерфейса в боте для загрузки эталона
- ❌ Не используется в обработке видео
- ❌ Формат данных может быть несовместим

---

### 8. Dashboard Generator (`app/reports/dashboard.py`)

**Класс:** `DashboardGenerator`

**Назначение:** Генерация визуального дашборда с метриками

**Что включает:**
- Паутинка метрик (matplotlib)
- Распределение нагрузки (столбчатая диаграмма)
- Зоны напряжения (горизонтальная диаграмма)
- Профиль скорости (график)
- Общая статистика (текст)

**Формат вывода:**
- PNG изображение (1920x1080)
- PDF (опционально)

**Проблемы:**
- ❌ Не генерируется или генерируется с ошибками
- ❌ Данные могут быть некорректными
- ❌ Не отправляется пользователю

**Код:** `app/reports/dashboard.py` (383 строки)

---

## 🔄 ПОТОК ОБРАБОТКИ ВИДЕО

### Последовательность операций:

```
1. Пользователь отправляет видео
   ↓
2. Видео сохраняется во временную директорию
   ↓
3. Пользователь выбирает тип визуализации
   ↓
4. Нажимает "✅ Готово"
   ↓
5. VideoProcessor.process_video() запускается
   ↓
6. Для каждого кадра:
   ├─ MediaPipe детектирует позу
   ├─ FrameAnalyzer анализирует кадр
   ├─ BoulderVisionMetrics вычисляет метрики движения
   ├─ VideoOverlays обновляет историю
   ├─ VideoOverlays применяет визуализацию
   └─ Кадр записывается в выходное видео
   ↓
7. После обработки всех кадров:
   ├─ Генерируется статистика
   ├─ Выполняется анализ напряжения
   ├─ Выполняется прогноз травм
   ├─ Выполняется HR анализ (nine-box)
   ├─ Генерируется дашборд (если возможно)
   └─ Результат возвращается
   ↓
8. Результат отправляется пользователю:
   ├─ Обработанное видео
   ├─ Дашборд (если создан)
   └─ Кнопки для дальнейших действий
```

---

## 📊 МЕТРИКИ И РАСЧЕТЫ

> 📋 **НОВАЯ КОНЦЕПЦИЯ:** Система переходит на 7 базовых метрик техники по методологии Eric J. Hörst «Training for Climbing». См. `files/ALGORITHM_SPEC.md` для полной спецификации.

### 🎯 7 базовых метрик техники (новая концепция)

Эти метрики основаны на признанных методологиях обучения скалолазанию:
- **Eric J. Hörst** — «Training for Climbing» (экономичность движений, quiet feet, hip position)
- **John Kettle** — «Coaching Climbing» (техника наблюдения)
- **Self-Coached Climber** — Dan Hague & Douglas Hunter (диагональное движение, противовес)
- **Movement for Climbers** — анализ паттернов движения

#### 1. Quiet Feet (Точность ног) — `quiet_feet`

**Что это:** Умение ставить ногу сразу в правильное место, без переставления и ёрзания.

**Как измеряем:**
- Количество перестановок ноги на одной точке опоры
- Кластеризация позиций стопы по близости (threshold = 20 пикселей)
- Среднее количество перестановок на зацепку

**Шкала оценки:**
- < 1.5 перестановок/зацепку → 90-100% (Отлично)
- 1.5 - 2.5 → 70-89% (Хорошо)
- 2.5 - 4.0 → 50-69% (Средне)
- > 4.0 → 0-49% (Слабо)

**Нормы для уровней:**
- 5-6a: допустимо 3-4 перестановки
- 6b-7a: норма < 2.5
- 7b+: норма < 1.5

---

#### 2. Hip Position (Положение таза) — `hip_position`

**Что это:** Насколько близко таз находится к стене во время лазания.

**Как измеряем:**
- Угол отклонения линии плечи-таз от вертикали
- Учитывается глубина (Z координата) — чем больше hip_center.z относительно shoulder_center.z, тем дальше таз

**Шкала оценки:**
- < 10° → 90-100% (Отлично)
- 10° - 20° → 70-89% (Хорошо)
- 20° - 35° → 50-69% (Средне)
- > 35° → 0-49% (Слабо, «висит на руках»)

---

#### 3. Противовес (Diagonal Movement) — `diagonal`

**Что это:** Координация противоположных конечностей — правая рука ↔ левая нога, левая рука ↔ правая нога.

**Как измеряем:**
- Корреляция движений противоположных конечностей
- Корреляция: правая рука ↔ левая нога
- Корреляция: левая рука ↔ правая нога
- Среднее значение корреляций

**Шкала оценки:**
- > 0.8 → 90-100% (Отлично, чёткая диагональ)
- 0.6 - 0.8 → 70-89% (Хорошо)
- 0.4 - 0.6 → 50-69% (Средне, часто «квадратом»)
- < 0.4 → 0-49% (Слабо, хаотичные движения)

---

#### 4. Считывание (Route Reading) — `route_reading`

**Что это:** Способность заранее спланировать движения — паузы для просмотра маршрута.

**Как измеряем:**
- Время до первого движения (смотрит маршрут)
- Количество «пауз взгляда» во время лазания (1-3 сек с поднятой головой)
- Нормализация по длительности маршрута

**Шкала оценки:**
- > 3 сек до старта + 3+ паузы → 90-100% (Отлично)
- 2-3 сек + 1-2 паузы → 70-89% (Хорошо)
- 1-2 сек + редкие паузы → 50-69% (Средне)
- Сразу стартует, без пауз → 0-49% (Слабо)

---

#### 5. Ритм (Movement Rhythm) — `rhythm`

**Что это:** Равномерность темпа движений — без резких ускорений и замедлений.

**Как измеряем:**
- Стабильность интервалов между движениями
- Коэффициент вариации (CV = std / mean)
- CV < 0.2 = отлично, CV > 0.5 = плохо

**Шкала оценки:**
- ±100 мс → 90-100% (Отлично, метроном)
- ±200 мс → 70-89% (Хорошо)
- ±350 мс → 50-69% (Средне)
- > ±350 мс → 0-49% (Слабо, хаотично)

---

#### 6. Контроль динамики (Dynamic Control) — `dynamic_control`

**Что это:** Качество выполнения динамических движений (бросков) — точность приземления и скорость стабилизации.

**Как измеряем:**
- Время стабилизации после приземления (мс)
- Отклонение от целевой точки (пиксели)
- Оценка стабилизации (< 500мс = отлично)
- Оценка точности (< 30px = отлично)

**Шкала оценки:**
- < 0.5 сек → 90-100% (Отлично)
- 0.5 - 1.0 сек → 70-89% (Хорошо)
- 1.0 - 2.0 сек → 50-69% (Средне)
- > 2.0 сек → 0-49% (Слабо, теряет контроль)

---

#### 7. Grip Release (Мягкость перехватов) — `grip_release`

**Что это:** Плавность отпускания зацепов — без рывков и резких движений.

**Как измеряем:**
- Анализ траектории руки при отпускании (первые 200мс)
- Производная скорости (ускорение)
- Количество резких пиков ускорения (jerk_count)
- Плавность = отсутствие рывков

**Шкала оценки:**
- Без рывков → 90-100% (Отлично)
- 1-2 рывка/перехват → 70-89% (Хорошо)
- 3-4 рывка/перехват → 50-69% (Средне)
- > 4 рывков/перехват → 0-49% (Слабо)

---

### 📈 Дополнительные метрики

#### Стабильность (`stability`)
- Дисперсия положения центра масс за скользящее окно (30 кадров)
- Низкая дисперсия = высокий балл
- > 80%: Отличный контроль тела
- 60-80%: Хорошая стабильность
- 40-60%: Заметное дрожание
- < 40%: Сильная нестабильность

#### Истощение (`exhaustion`)
- Сравнение качества движений в начале и конце маршрута
- Делим маршрут на 4 части, сравниваем первую и последнюю четверть
- < 20%: Минимальное истощение
- 20-40%: Умеренное истощение
- 40-60%: Заметное падение качества
- > 60%: Сильное истощение, красная зона

#### Эффективность рук (`arm_efficiency`)
- Процент веса, который несут руки
- Идеал: 30-40%
- Проблема: > 50%

#### Эффективность ног (`leg_efficiency`)
- Процент веса, который несут ноги
- Идеал: 60-70%
- Проблема: < 40%

#### Восстановление (`recovery`)
- Умение находить позиции для отдыха
- Анализ пауз: расслабление мышц, длительность (2-5 сек оптимально), выпрямление рук
- > 80%: Отлично умеет восстанавливаться
- 60-80%: Делает паузы, но не всегда эффективно
- 40-60%: Редко отдыхает или отдых неэффективен
- < 40%: Не умеет восстанавливаться

---

### 🕸️ Паутинка (Spider Chart)

**7 осей:**
1. QF — Quiet Feet
2. HP — Hip Position
3. ПВ — Противовес
4. СЧ — Считывание
5. РТ — Ритм
6. ДН — Динамика (Dynamic Control)
7. GR — Grip Release

**Параметры отображения:**
- Позиция: верхний левый угол (50, 50)
- Радиус: 80 пикселей
- Фон: полупрозрачный круг (alpha = 0.3)
- Цвет заливки: (0, 200, 255) с alpha = 0.4
- Общий балл внизу: среднее всех метрик

---

### ⚠️ УСТАРЕВШИЕ МЕТРИКИ (требуют замены)

> ⚠️ **ВНИМАНИЕ:** Следующие метрики используются в текущей реализации, но должны быть заменены на новые 7 базовых метрик:

#### Сила (`_calculate_strength()`) — УСТАРЕЛО
- Используется в текущей реализации
- Должна быть заменена на комбинацию метрик (Hip Position + Grip Release)

#### Баланс (`_calculate_balance()`) — УСТАРЕЛО
- Используется в текущей реализации
- Должен быть заменен на Противовес (Diagonal Movement)

#### Координация (`_calculate_coordination()`) — УСТАРЕЛО
- Используется в текущей реализации
- Должна быть заменена на Противовес (Diagonal Movement)

#### Техника (`_calculate_technique()`) — УСТАРЕЛО
- Используется в текущей реализации
- Должна быть заменена на среднее 7 базовых метрик

### Распределение нагрузки (`_calculate_weight_distribution()`)

```python
# Высота конечностей относительно центра масс (бёдер)
left_arm_height = hip_y - left_wrist.y
right_arm_height = hip_y - right_wrist.y
left_leg_support = left_ankle.y - hip_y
right_leg_support = right_ankle.y - hip_y

# Нормализация
total = abs(left_arm_height) + abs(right_arm_height) + 
        abs(left_leg_support) + abs(right_leg_support)

# Распределение
left_arm = abs(left_arm_height) / total * 100 * 1.5
right_arm = abs(right_arm_height) / total * 100 * 1.5
left_leg = abs(left_leg_support) / total * 100 * 1.2
right_leg = abs(right_leg_support) / total * 100 * 1.2

# Нормализация до 100%
```

### Скорость (`_calculate_velocity()`)

```python
# Смещение ключевых точек между кадрами
prev = position_history[-2]
curr = position_history[-1]

# Для каждой точки (левое/правое запястье, нос)
dx = curr[key][0] - prev[key][0]
dy = curr[key][1] - prev[key][1]
distance = sqrt(dx² + dy²)

velocity = среднее_расстояние / количество_точек
```

---

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### Критические проблемы:

1. **Вопросительные знаки в визуализации**
   - **Где:** Паутинка метрик, нагрузка, легенды
   - **Причина:** Значения None/NaN не обрабатываются корректно
   - **Статус:** Частично исправлено (только для нагрузки)

2. **Паутинка метрик не работает**
   - **Проблема:** Показывает вопросы или пустую диаграмму
   - **Причина:** Метрики не вычисляются или содержат None/NaN
   - **Статус:** Не исправлено

3. **Дашборд не генерируется**
   - **Проблема:** Не создается или создается с ошибками
   - **Причина:** Ошибки в dashboard.py или некорректные данные
   - **Статус:** Не исправлено

4. **Призрак не работает**
   - **Проблема:** Не загружается или не отображается
   - **Причина:** Нет интерфейса для загрузки, формат данных может быть несовместим
   - **Статус:** Не исправлено

5. **Полный анализ не включает все компоненты**
   - **Проблема:** Скорость и зажимы не отображаются
   - **Причина:** Код добавлен, но может не работать корректно
   - **Статус:** Частично исправлено (код добавлен, но не протестирован)

6. **Легенда внизу показывает вопросы**
   - **Проблема:** Внизу экрана легенда с вопросами
   - **Причина:** Некорректные значения при форматировании
   - **Статус:** Не исправлено

### Частично исправленные:

1. **Нагрузка показывает кг вместо процентов**
   - **Статус:** ✅ Исправлено - теперь показывает только проценты
   - **Осталось:** Проверить легенду внизу

---

## 🔗 ВЗАИМОДЕЙСТВИЕ КОМПОНЕНТОВ

### Схема взаимодействия:

```
Telegram Bot (handlers.py)
    ↓
VideoProcessor (processor.py)
    ├─→ MediaPipe Pose (детекция скелета)
    ├─→ FrameAnalyzer (анализ кадров)
    ├─→ BoulderVisionMetrics (метрики движения)
    ├─→ VideoOverlays (визуализация)
    │   ├─→ draw_spider_metrics()
    │   ├─→ draw_weight_load()
    │   ├─→ draw_tension_zones()
    │   ├─→ draw_speed_map()
    │   ├─→ draw_ideal_ghost()
    │   └─→ draw_full_analysis()
    ├─→ BodyTensionAnalyzer (напряжение)
    ├─→ InjuryPredictor (прогноз травм)
    ├─→ ClimberNineBoxModel (HR модель)
    ├─→ RouteAssessor (оценка трассы)
    └─→ DashboardGenerator (дашборд)
```

### Поток данных:

```
Video Frame (BGR)
    ↓
MediaPipe → landmarks (33 точки)
    ↓
FrameAnalyzer → frame_data {
    pose_quality: float,
    motion_intensity: float,
    angles: dict,
    center_of_mass: dict,
    balance_score: float
}
    ↓
BoulderVisionMetrics → bv_metrics {
    velocity_ratio: float,
    cumulative_distance: float,
    trajectory_similarity: float
}
    ↓
VideoOverlays._update_history() → обновляет:
    - metrics_history
    - position_history
    - tension_history
    - velocity_history
    ↓
VideoOverlays.apply_overlay() → рисует на кадре
    ↓
Output Frame (BGR с визуализацией)
```

---

## 📝 ДЕТАЛЬНОЕ ОПИСАНИЕ ФУНКЦИЙ

### VideoOverlays методы:

#### `_update_history()` (строки 150-191)
**Назначение:** Обновление истории для всех типов анализа

**Что делает:**
1. Сохраняет позиции ключевых точек (запястья, лодыжки, бёдра, нос)
2. Вычисляет текущие метрики через `_calculate_current_metrics()`
3. Вычисляет напряжение через `_calculate_tension()`
4. Вычисляет скорость через `_calculate_velocity()`
5. Обновляет индекс кадра

**Проблемы:**
- Если landmarks None, история не обновляется
- Метрики могут быть None/NaN

#### `_calculate_current_metrics()` (строки 193-213)
**Назначение:** Вычисление метрик для паутинки

**Что делает:**
1. Вызывает `_calculate_strength()` - оценка силы
2. Вызывает `_calculate_balance()` - оценка баланса
3. Вызывает `_calculate_coordination()` - оценка координации
4. Вызывает `_calculate_technique()` - оценка техники
5. Валидирует все значения (проверка на None/NaN)

**Возвращает:**
```python
{
    'сила': float (0-100),
    'баланс': float (0-100),
    'координация': float (0-100),
    'техника': float (0-100)
}
```

#### `_calculate_weight_distribution()` (строки 677-730)
**Назначение:** Расчёт распределения веса по конечностям

**Алгоритм:**
1. Получает позиции запястий, лодыжек, бёдер
2. Вычисляет центр масс (бёдра)
3. Вычисляет высоту конечностей относительно ЦМ
4. Нормализует до 100%

**Возвращает:**
```python
{
    'left_arm': float (0-100),
    'right_arm': float (0-100),
    'left_leg': float (0-100),
    'right_leg': float (0-100)
}
```

**Проблемы:**
- Может возвращать None или некорректные значения
- Алгоритм может быть неточным

---

## 🎯 ТЕКУЩЕЕ СОСТОЯНИЕ

### Что работает:
- ✅ Загрузка видео через бота
- ✅ Выбор типа визуализации
- ✅ Обработка видео через MediaPipe
- ✅ Нагрузка показывает проценты (исправлено)
- ✅ Базовое отображение скелета

### Что не работает:
- ❌ Паутинка метрик (вопросительные знаки)
- ❌ Дашборд не генерируется
- ❌ Призрак не работает
- ❌ Полный анализ не включает все компоненты
- ❌ Легенда внизу показывает вопросы
- ❌ Скорость и зажимы в полном анализе не отображаются

### Что частично работает:
- ⚠️ Нагрузка (исправлено отображение, но могут быть проблемы с расчетом)
- ⚠️ Полный анализ (код добавлен, но не протестирован)

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Формат данных landmarks (MediaPipe):

MediaPipe возвращает объект с 33 ключевыми точками:
- 0: нос
- 11-12: плечи
- 13-14: локти
- 15-16: запястья
- 23-24: бёдра
- 25-26: колени
- 27-28: лодыжки

Каждая точка имеет:
- `x`, `y`, `z` - координаты (нормализованные 0-1)
- `visibility` - видимость (0-1)

### Формат frame_data:

```python
{
    'frame_number': int,
    'timestamp': float,
    'pose_quality': float (0-100),
    'motion_intensity': float (0-40+),
    'angles': {
        'left_elbow': float,
        'right_elbow': float,
        'left_shoulder': float,
        'right_shoulder': float,
        ...
    },
    'center_of_mass': {
        'x': float,
        'y': float
    },
    'balance_score': float (0-100),
    'valid': bool
}
```

### Формат данных для дашборда:

```python
{
    'duration': float,
    'avg_pose_quality': float,
    'avg_motion_intensity': float,
    'avg_balance_score': float,
    'fall_detected': bool,
    'bouldervision': {
        'velocity_history': List[float]
    },
    'tension_analysis': {
        'zones': Dict[str, float]
    },
    'weight_distribution': Dict[str, float],
    'metrics': {
        'сила': float,
        'баланс': float,
        'координация': float,
        'техника': float
    }
}
```

---

## 📌 ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Вес пользователя:** По умолчанию 70 кг, но не запрашивается у пользователя. Используется только для расчета нагрузки в кг (который теперь не показывается).

2. **История метрик:** Хранится только последние 90 кадров (3 секунды при 30 FPS). Это может быть недостаточно для полного анализа.

3. **Валидация данных:** Не все функции проверяют None/NaN перед использованием, что приводит к ошибкам отображения.

4. **Формат призрака:** Данные призрака должны быть в формате списка словарей с ключами 'x', 'y', 'visibility', но формат может отличаться.

5. **Дашборд:** Генерируется только если все данные корректны. При ошибках просто не создается (не падает с ошибкой).

---

## 🚀 РЕКОМЕНДАЦИИ ДЛЯ УЛУЧШЕНИЯ

1. **Добавить валидацию везде:** Проверять None/NaN перед использованием значений
2. **Улучшить расчет метрик:** Более точные алгоритмы для силы, баланса, координации
3. **Добавить интерфейс для призрака:** Кнопка в боте для загрузки эталонного видео
4. **Исправить дашборд:** Убедиться, что он генерируется и отправляется пользователю
5. **Протестировать полный анализ:** Убедиться, что все компоненты отображаются
6. **Улучшить легенду:** Исправить отображение вопросов внизу экрана

---

---

## 📚 ИСТОРИЯ РАЗРАБОТКИ И ВАРИАНТЫ РЕАЛИЗАЦИИ

### Обзор эволюции проекта

Проект ClimbAI прошел через множество итераций и вариантов реализации. Ниже описаны все найденные варианты с их особенностями, технологиями и статусом.

---

### 1. 🖥️ DESKTOP APPLICATION (Десктопное приложение)

**Расположение:** `/home/user/с винды/ClimbAI/desktop_app/`

**Описание:** Полнофункциональное десктопное приложение на Python с графическим интерфейсом.

**Технологии:**
- **GUI Framework:** Tkinter с кастомными темами
- **Анализ:** MediaPipe Holistic (543 landmarks)
- **AI:** GPT-4 и Claude API
- **Упаковка:** PyInstaller для создания .exe файлов
- **Установщик:** Inno Setup для Windows

**Архитектура:**
```
desktop_app/
├── src/
│   ├── ui/
│   │   ├── modern_app.py          # Современный интерфейс (ModernClimbAIApp)
│   │   ├── liquid_glass_app.py     # Альтернативный стиль интерфейса
│   │   └── themes.py               # Темы оформления
│   ├── ai/
│   │   ├── ai_analyzer.py          # Интеграция GPT-4 и Claude
│   │   └── claude_prompt_template.py
│   ├── analysis/
│   │   ├── climbing_pose_estimator.py
│   │   ├── fms_analyzer.py         # Functional Movement Screen анализ
│   │   ├── movement_classifier.py
│   │   ├── detailed_frame_analyzer.py
│   │   └── enhanced_analyzers.py
│   ├── core/
│   │   ├── biomechanical_analyzer.py
│   │   ├── full_body_analyzer.py
│   │   ├── predictive_analyzer.py
│   │   └── video_analyzer.py
│   ├── center_of_mass/
│   │   └── com_calculator.py      # Расчет центра масс
│   └── experts/
│       └── climbing_experts.py    # Экспертные методологии
├── build/                         # Собранные файлы PyInstaller
├── dist/                          # Готовые .exe файлы
└── ClimbAI_Setup.iss              # Скрипт установщика
```

**Особенности:**
- ✅ Покадровый анализ с навигацией
- ✅ Кнопки "Лучший/Худший кадр"
- ✅ Центр тяжести с визуализацией
- ✅ 8 форматов отчетов (технический, клиентский, детектив, геймерский и др.)
- ✅ CSV экспорт данных
- ✅ Предиктивная аналитика травм
- ✅ Психологический профиль через AI
- ✅ Экспертный анализ (Eric Hörst, Magnus Midtbø, Neil Gresham, Dave MacLeod)

**Методологии анализа:**
1. **Efficiency Analysis (Eric Hörst)** - анализ эффективности движений
2. **Dynamic Movement Flow (Magnus Midtbø)** - потоковость движений
3. **Problem-solving Approach (Neil Gresham)** - подход к решению проблем
4. **Systematic Skill Development (Dave MacLeod)** - системное развитие навыков

**Известные проблемы (исправленные):**
- ❌ Прогресс-бар показывал 60% и исчезал → ✅ Исправлено с промежуточными этапами
- ❌ Эффективность всегда 100% → ✅ Исправлена формула (10-85%)
- ❌ Пустой вердикт → ✅ Восстановлен заголовок "⚡ МГНОВЕННЫЙ ВЕРДИКТ"
- ❌ Центр тяжести исчезал → ✅ Исправлен расчет и отображение

**Статус:** ✅ Работает, есть готовые .exe файлы

---

### 2. 📱 MOBILE APPLICATION (Мобильное приложение)

**Расположение:** `/home/user/с винды/ClimbAI/mobile_app/`

**Описание:** Кроссплатформенное мобильное приложение для Android на базе Kivy/KivyMD.

**Технологии:**
- **UI Framework:** KivyMD (Material Design для Kivy)
- **Анализ:** MediaPipe (оптимизированная версия)
- **AI:** Claude API
- **Сборка:** Buildozer для создания APK
- **Docker:** Для сборки APK в контейнере

**Архитектура:**
```
mobile_app/
├── src/                           # Общий код с desktop версией
│   ├── ui/                        # Адаптированный UI для мобильных
│   ├── ai/                        # AI модули
│   ├── analysis/                  # Анализ
│   └── core/                      # Основные компоненты
├── main.py                        # Точка входа мобильного приложения
├── buildozer.spec                 # Конфигурация сборки
├── Dockerfile                     # Docker для сборки APK
└── climbai_mobile_ready_v1.0.apk  # Готовый APK файл
```

**Особенности:**
- ✅ Съемка видео прямо в приложении
- ✅ Выбор файлов из галереи
- ✅ Анализ в реальном времени
- ✅ Адаптивный интерфейс для мобильных устройств
- ✅ Оптимизация для Android 7.0+ (API 21+)

**Системные требования:**
- Android 7.0+ (API 21+)
- RAM: 3GB+ (рекомендуется 4GB+)
- Свободное место: 500MB
- Интернет для AI анализа

**Статус:** ✅ Есть готовый APK файл (`climbai_mobile_ready_v1.0.apk`)

---

### 3. 🤖 TELEGRAM BOT (Текущая версия)

**Расположение:** `/home/user/с винды/ClimbAI/telegram_bot_bouldervision/`

**Описание:** Telegram бот для анализа видео скалолазания с различными типами визуализации.

**Технологии:**
- **Bot Framework:** python-telegram-bot v20.7
- **Анализ:** MediaPipe Pose (33 ключевые точки)
- **AI:** Anthropic Claude API
- **База данных:** SQLite через SQLAlchemy
- **Деплой:** Railway (ранее использовался)

**Особенности:**
- ✅ 6 типов визуализации (паутинка, нагрузка, зажимы, скорость, призрак, полный анализ)
- ✅ 8 форматов отчетов
- ✅ Дашборд с метриками (PNG/PDF)
- ✅ Алгоритмический анализ без AI
- ✅ Система призраков (эталонные видео)
- ✅ Детекция падений
- ✅ Прогноз травм
- ✅ HR модель (Nine-Box)

**Статус:** ⚠️ Частично работает (см. раздел "Известные проблемы")

---

### 4. 🤖 TELEGRAM BOT (Старая версия)

**Расположение:** `/home/user/с винды/ClimbAI/telegram bot new/`

**Описание:** Предыдущая версия Telegram бота с другой архитектурой.

**Особенности:**
- Базовая обработка видео
- Простые визуализации (скелет, точки, напряжение)
- Интеграция с Railway для деплоя
- Кэширование промптов для Claude API

**Статус:** ⚠️ Устаревшая версия, заменена на `telegram_bot_bouldervision`

---

### 5. 📊 BOULDERVISION REFERENCE (Референсная реализация)

**Расположение:** `/home/user/с винды/ClimbAI/bouldervision_reference/`

**Описание:** Референсная реализация BoulderVision от Daniel Reiff - оригинальная система анализа движений скалолазов.

**Технологии:**
- **Computer Vision:** OpenCV, Supervision
- **Pose Detection:** Roboflow Inference SDK (workflow)
- **Hold Detection:** Roboflow API
- **Visualization:** Matplotlib, Plotly
- **Metrics:** Velocity Ratio, Cumulative Distance, Trajectory Similarity

**Метрики BoulderVision:**

#### 1. Velocity Ratio (Коэффициент скорости)
- Сравнение текущей скорости с средней за окно истории
- `> 1`: Движение быстрее среднего
- `= 1`: Движение на среднем уровне
- `< 1`: Движение медленнее среднего

#### 2. Cumulative Distance (Накопленное расстояние)
- Общее расстояние, пройденное всеми ключевыми точками
- Измеряется в пикселях
- Высокие значения = динамичное движение
- Низкие значения = статичные позиции

#### 3. Trajectory Similarity (Схожесть траектории)
- Косинусное сходство текущего движения с ожидаемым путем
- Диапазон: [-1, 1]
- `1`: Движения идентичны по направлению
- `0`: Движения перпендикулярны
- `-1`: Движения в противоположных направлениях

**Архитектура:**
```python
class ClimbingAnalyzer:
    - run_inference()          # Запуск workflow через Roboflow
    - process_frame()          # Обработка кадра
    - update_plots()           # Обновление графиков Plotly
    - create_circular_sticker() # Создание логотипа
```

**Визуализация:**
- Трехпанельный вид: отслеживание, ключевые точки, тепловая карта
- Графики в реальном времени: Cumulative Movement, Velocity Ratio
- Детекция зацепов с боксами
- Сопоставление точек тела с зацепами

**Статус:** ✅ Референсная реализация, используется как основа для метрик

**Связь с текущей версией:**
- Метрики BoulderVision интегрированы в `app/bouldervision/metrics.py`
- Velocity Ratio используется в текущей системе
- Trajectory Similarity используется для анализа траектории

---

### 6. 💾 BEST VERSION BACKUP (Резервная копия лучшей версии)

**Расположение:** `/home/user/с винды/ClimbAI/best_version_backup/`

**Описание:** Резервная копия стабильной версии десктопного приложения.

**Содержимое:**
- Полная структура `src/` с модулями
- UI компоненты (`modern_app.py`, `liquid_glass_app.py`)
- AI анализаторы
- Экспертные модули
- Анализаторы движений

**Статус:** ✅ Резервная копия для восстановления

---

### 7. 📦 ARCHIVE OLD FILES (Архив старых файлов)

**Расположение:** `/home/user/с винды/ClimbAI/archive_old_files/`

**Описание:** Архив всех предыдущих версий, экспериментов и тестовых файлов.

**Содержимое:**
- Множество версий `climbai.py`, `climbai_*.py`
- Тестовые скрипты (`test_*.py`)
- Отчеты об исправлениях (`FIXES_REPORT.md`, `FINAL_REPORT.md`)
- Инструкции по запуску (`ЗАПУСК*.md`)
- Технические задания (PDF)
- Резервные копии архитектуры

**Интересные находки:**

#### backup_architecture_version/
- Старая архитектура с другой структурой модулей
- Альтернативные реализации анализаторов

#### backup_working_version/
- Рабочая версия с исправлениями
- Стабильная реализация UI

#### ClimbAI_Fixed/
- Исправленная версия с багфиксами
- Альтернативные подходы к решению проблем

**Статус:** 📚 Архив для изучения истории разработки

---

### 8. 🖥️ DESKTOP APP КОПИИ (Альтернативные сборки)

**Расположение:** 
- `/home/user/с винды/ClimbAI/desktop_app — копия/`
- `/home/user/с винды/ClimbAI/desktop_app — копия — копия/`

**Описание:** Альтернативные версии десктопного приложения с разными конфигурациями.

**Особенности:**
- Разные версии UI
- Альтернативные сборки PyInstaller
- Разные конфигурации зависимостей
- Инструкции по сборке (`BUILD_INSTRUCTIONS.md`, `ВЫБОР_СБОРКИ.md`)

**Статус:** ⚠️ Экспериментальные версии

---

### 9. 🌐 WEB APPLICATION (Веб-приложение)

**Упоминается в:** `README.md`, `archive_old_files/web_app.py`

**Описание:** Веб-приложение на Flask для анализа видео через браузер.

**Технологии:**
- **Backend:** Flask
- **Frontend:** HTML/CSS/JavaScript
- **Анализ:** MediaPipe (через Python backend)
- **API:** REST API для загрузки и анализа видео

**Endpoints (из документации):**
- `POST /upload` - загрузка видео
- `POST /analyze` - запуск анализа
- `POST /ai_analyze` - AI анализ результатов
- `GET /results/{result_id}` - получение результатов

**Статус:** ⚠️ Упоминается, но не найдена активная версия

---

### 10. 🏢 SYNOLOGY OPTIMIZED (Версия для Synology)

**Упоминается в:** ZIP архивы (`ClimbAI_Synology_*.zip`)

**Описание:** Оптимизированная версия для запуска на Synology NAS.

**Особенности:**
- Оптимизация для ARM процессоров
- Адаптация под ограниченные ресурсы NAS
- Упрощенный интерфейс
- Оптимизация MediaPipe для слабого железа

**Статус:** 📦 Упакована в ZIP архивы, не развернута

---

## 🔄 ЭВОЛЮЦИЯ АРХИТЕКТУРЫ

### Этап 1: Монолитная версия
- Один файл `climbai.py` со всей логикой
- Простой GUI на Tkinter
- Базовый анализ через MediaPipe

### Этап 2: Модульная архитектура
- Разделение на модули (`src/ai/`, `src/analysis/`, `src/core/`)
- Экспертные анализаторы
- Улучшенный UI с темами

### Этап 3: Мультиплатформенность
- Desktop приложение
- Мобильное приложение (Kivy)
- Веб-приложение (Flask)
- Telegram бот

### Этап 4: Специализация (текущий этап)
- **Telegram Bot** - основной продукт с визуализациями
- **BoulderVision метрики** - интеграция референсной реализации
- **Алгоритмический анализ** - без зависимости от AI
- **Дашборды** - визуализация метрик

---

## 📊 СРАВНЕНИЕ ВАРИАНТОВ

| Вариант | Платформа | UI | Анализ | AI | Статус |
|---------|-----------|----|----|----|--------|
| Desktop App | Windows/Linux/Mac | Tkinter | MediaPipe Holistic | GPT-4, Claude | ✅ Работает |
| Mobile App | Android | KivyMD | MediaPipe (оптимизирован) | Claude | ✅ APK готов |
| Telegram Bot (текущий) | Telegram | Inline кнопки | MediaPipe Pose | Claude | ⚠️ Частично |
| Telegram Bot (старый) | Telegram | Inline кнопки | MediaPipe | Claude | ⚠️ Устарел |
| BoulderVision Ref | Desktop | Matplotlib/Plotly | Roboflow Workflow | Нет | ✅ Референс |
| Web App | Браузер | HTML/CSS/JS | MediaPipe (backend) | GPT-4, Claude | ⚠️ Не найдена |

---

## 🎯 КЛЮЧЕВЫЕ РАЗЛИЧИЯ В РЕАЛИЗАЦИЯХ

### Desktop vs Mobile vs Telegram Bot

#### Desktop Application:
- **Преимущества:**
  - Полный контроль над UI
  - Более мощное железо для анализа
  - Богатые визуализации
  - Офлайн работа (кроме AI)
- **Недостатки:**
  - Требует установки
  - Только локальное использование
  - Зависимость от ОС

#### Mobile Application:
- **Преимущества:**
  - Удобство съемки видео
  - Мобильность
  - Нативный опыт
- **Недостатки:**
  - Ограниченные ресурсы
  - Сложность сборки APK
  - Зависимость от версии Android

#### Telegram Bot:
- **Преимущества:**
  - Не требует установки
  - Доступность с любого устройства
  - Простота использования
  - Централизованное хранение данных
- **Недостатки:**
  - Ограничения Telegram API
  - Зависимость от интернета
  - Ограничения размера файлов

---

## 🔍 ИНТЕРЕСНЫЕ НАХОДКИ В АРХИВЕ

### Альтернативные UI стили:
1. **Modern Theme** - современный минималистичный дизайн
2. **Liquid Glass Theme** - стеклянный эффект с прозрачностью
3. **Dark Theme** - темная тема для комфортной работы

### Альтернативные анализаторы:
1. **Enhanced Analyzer** - улучшенный анализатор с дополнительными метриками
2. **Detailed Frame Analyzer** - детальный покадровый анализ
3. **FMS Analyzer** - анализ по методологии Functional Movement Screen
4. **Predictive Analyzer** - предиктивная аналитика

### Экспериментальные функции:
1. **Champions Database** - база данных чемпионов для сравнения
2. **Boulder2Vec** - векторное представление движений
3. **Expert API** - API для экспертного анализа
4. **External Models** - интеграция внешних ML моделей

---

## 📝 ВЫВОДЫ

1. **Проект прошел долгий путь развития** - от монолитного скрипта до мультиплатформенного решения
2. **Множество итераций** - каждая версия улучшала предыдущую
3. **Разные подходы к UI** - от простого Tkinter до современных тем
4. **Эволюция анализа** - от базового MediaPipe до комплексного анализа с AI
5. **Специализация** - текущий фокус на Telegram боте с визуализациями
6. **Интеграция BoulderVision** - использование референсной реализации для метрик

**Рекомендации:**
- Использовать опыт Desktop версии для улучшения Telegram бота
- Интегрировать лучшие практики из Mobile версии
- Изучить BoulderVision reference для улучшения метрик
- Взять стабильные компоненты из best_version_backup

---

## 🆕 НОВАЯ КОНЦЕПЦИЯ (MVP 1.0)

> ⚠️ **ВАЖНО:** Система переходит на новую концепцию на основе файлов в `files/`. Текущая реализация частично работает, но требует переработки.

### 📋 Источники новой концепции

В папке `files/` находятся три ключевых файла:

1. **`ALGORITHM_SPEC.md`** — Полная спецификация алгоритмов с 7 базовыми метриками техники
2. **`TEXT_TEMPLATES.md`** — Текстовые шаблоны для генерации отчётов без ИИ
3. **`dashboard_prototype.html`** — Визуальный прототип дашборда

### 🎯 7 базовых метрик техники (новая концепция)

Вместо старых метрик (Сила, Баланс, Координация, Техника) используются:

1. **Quiet Feet (Точность ног)** — количество перестановок на зацепку
2. **Hip Position (Положение таза)** — угол отклонения от стены
3. **Противовес (Diagonal Movement)** — координация противоположных конечностей
4. **Считывание (Route Reading)** — паузы для просмотра маршрута
5. **Ритм (Movement Rhythm)** — равномерность темпа движений
6. **Контроль динамики (Dynamic Control)** — качество после бросков
7. **Grip Release (Перехваты)** — плавность отпускания зацепов

**Методологическая база:**
- Eric J. Hörst «Training for Climbing»
- John Kettle «Coaching Climbing»
- Self-Coached Climber (Dan Hague & Douglas Hunter)
- Movement for Climbers

### 📊 Дополнительные метрики

- **Стабильность** — контроль центра масс
- **Истощение** — падение качества к концу маршрута
- **Эффективность рук** — процент нагрузки на руки (норма: 30-40%)
- **Эффективность ног** — процент нагрузки на ноги (норма: 60-70%)
- **Восстановление** — умение находить позы для отдыха

### 🎨 Новая визуализация

**Цветной скелет:**
- Линии окрашиваются по зонам напряжения
- Зелёный (< 30) → Жёлтый (30-60) → Красный (> 60)
- 7 зон: плечи (2), локти (2), поясница, колени (2)

**Паутинка:**
- 7 осей вместо 4
- Оси: QF, HP, ПВ, СЧ, РТ, ДН, GR
- Радиус: 80 пикселей
- Позиция: верхний левый угол

### 📋 SWOT-анализ

**Логика:**
- **Strengths:** метрики ≥ 75% (макс. 4)
- **Weaknesses:** метрики < 55% (макс. 4)
- **Opportunities:** выводятся из слабостей (макс. 3)
- **Threats:** риски травм на основе зон напряжения (макс. 3)

**Текстовые шаблоны:**
- Все тексты генерируются из `TEXT_TEMPLATES.md`
- 4 уровня для каждой метрики: excellent, good, medium, poor
- Плейсхолдеры для подстановки значений

### 📊 Дашборд

**Структура:**
1. Header — логотип, дата, длительность, кадры
2. Техника лазания — список метрик + паутинка + общий балл + уровень
3. SWOT-анализ — 4 карточки с цветовым кодированием
4. Дополнительные метрики — компактная строка с 5 показателями
5. Footer — методологическая справка, временная метка

**Формат:** PNG (1920×1080), PDF (опционально), HTML (прототип)

### 🔄 Статус перехода

**✅ Готово:**
- Спецификация алгоритмов (`files/ALGORITHM_SPEC.md`)
- Текстовые шаблоны (`files/TEXT_TEMPLATES.md`)
- Прототип дашборда (`files/dashboard_prototype.html`)

**⚠️ Требует переработки:**
- `app/video/overlays.py` — заменить старые метрики на новые 7 базовых
- `app/reports/dashboard.py` — переработать под новую структуру
- `app/analysis/` — добавить новые модули для 7 метрик

**❌ Не реализовано:**
- `app/analysis/technique_metrics.py` — новый модуль для 7 метрик
- `app/analysis/additional_metrics.py` — новый модуль для дополнительных метрик
- `app/analysis/swot_generator.py` — новый модуль с шаблонами
- `app/visualization/colored_skeleton.py` — новый модуль цветного скелета
- `app/visualization/spider_chart.py` — новый модуль паутинки с 7 осями

### 📝 Рекомендации по реализации

1. **Начать с метрик:** Реализовать `technique_metrics.py` с 7 базовыми метриками
2. **Обновить визуализацию:** Переработать `overlays.py` под новую концепцию
3. **Интегрировать шаблоны:** Загружать тексты из `TEXT_TEMPLATES.md`
4. **Обновить дашборд:** Переработать `dashboard.py` под новую структуру
5. **Тестирование:** Проверить все метрики на реальных видео

---

**Конец документа**
